# Audio Assets

Drop your dialog beep file here as:

- `assets/audio/dialog-word-beep.wav`

The renderer will attempt to play this file once per revealed word in pet replies.
If the file is missing or fails to load, the app falls back to a short synthesized beep.
